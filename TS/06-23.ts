interface SmartMoney {
  smell: string;        // 敏锐的嗅觉
  information: string;  // 先人一步察觉市场的信息
  endure: string;       // 忍耐力
}

/// class
class Point {
  x: number;
  y: number;
}

const pt = new Point()
pt.x = 10
pt.y = 20

export default function helloWorld() {
  console.log('hello world!');
  
}

// math

